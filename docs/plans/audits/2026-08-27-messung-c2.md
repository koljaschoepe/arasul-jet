# Messung, Phase C2 des Überordner-Plans vom 26.08.2026

Mitarbeiterverwaltung: anlegen, Passwort setzen, stilllegen, löschen, und die
Freigabe je App und Mitarbeiter in `app_members` (Migration 168), Nachfolgerin
von `space_members`. Alle neuen Endpunkte sind Admin-Endpunkte. Gezählt mit
`python3 scripts/test/zeilen.py` (die Messregel aus der Löschliste, siehe
[`2026-08-26-loeschliste-b1.md`](2026-08-26-loeschliste-b1.md), Frage 7).

## Vorher und nachher

Vorher ist der Stand `32881ce8` auf `main` (Basis des Branches `phase/c2`),
nachher der Kopf des Branches.

| Bereich                                          | vorher  | nachher | Differenz  |
| ------------------------------------------------ | ------- | ------- | ---------- |
| Frontend `apps/dashboard-frontend/src`           | 31 294  | 31 294  | 0          |
| Backend-Routen `src/routes`                      | 9 400   | 9 614   | +214       |
| Backend-Services `src/services`                  | 21 650  | 21 889  | +239       |
| Backend übrig                                    | 6 612   | 6 674   | +62        |
| Backend-Tests `__tests__`                        | 23 880  | 24 171  | +291       |
| Dienste und Container (`services/*`, `config/*`) | 29 231  | 29 271  | +40        |
| Skripte (`scripts/*`)                            | 22 777  | 23 089  | +312       |
| **Gesamt**                                       | 144 844 | 146 002 | **+1 158** |

Wie C1 baut diese Phase, sie baut nicht ab. Was dazukommt: zwei Endpunkte auf
`/api/benutzer`, der neue Router `/api/freigaben` und der Baustein
`istEigenesKonto` samt Begründung (+214 in den Routen), der Freigabe-Service
und die zwei neuen Bausteine im Benutzer- und Passwort-Service (+239), das
Zod-Schema (+62), zwei Testdateien (+291), Migration 168 (+40) sowie die neue
Abnahme und die Nachbesserung an der alten (+312).

**Zur Vergleichbarkeit:** `zeilen.py` zählt für `services/*` jede Textdatei und
erwischt dabei auch `.pytest_cache/README.md` und `.pytest_cache/.gitignore` —
Dateien, die erst nach einem lokalen `run-tests.sh --all` entstehen und in
`.gitignore` stehen. Gemessen mit ihnen kommen 100 Zeilen zu viel heraus
(29 371 statt 29 271). Die Zahlen oben sind mit `git clean -Xdf services/`
davor genommen. Steht unter „Unterwegs gefunden": eine Messregel, deren
Ergebnis davon abhängt, ob jemand vorher Tests laufen ließ, vergleicht zwei
Stände nicht mehr zuverlässig.

Das Frontend bleibt bei null: die Oberfläche für die Mitarbeiterverwaltung
kommt mit den D-Phasen, und eine Seite zu bauen, die die neue Oberfläche zwei
Phasen später wieder ersetzt, wäre doppelte Arbeit ohne Kunden dazwischen. Bis
dahin ist der Weg die Schnittstelle, beschrieben im Admin-Handbuch.

## Was neu ist

| Weg                                        | Wer   | Was                                                                                   |
| ------------------------------------------ | ----- | ------------------------------------------------------------------------------------- |
| `PUT /api/benutzer/:id/passwort`           | Admin | Passwort setzen, ohne das alte zu kennen; beendet alle Sitzungen des Betroffenen      |
| `PUT /api/benutzer/:id/aktiv`              | Admin | Stilllegen (`false`) und wieder zulassen (`true`)                                     |
| `GET /api/freigaben`                       | Admin | Alle Freigaben, Filter `?app_id=` und `?benutzer_id=`                                 |
| `POST /api/freigaben`                      | Admin | Freigeben; 201 neu, 200 wenn sie schon stand                                          |
| `DELETE /api/freigaben/:appId/:benutzerId` | Admin | Zurücknehmen; 404, wenn es die Freigabe nicht gibt                                    |
| `POST /api/auth/change-password`           | beide | Bestand aus C1: das EIGENE Passwort, mit Prüfung des alten und den Komplexitätsregeln |

`app_members(app_id, user_id)` ersetzt `space_members` aus Migration 089. Die
alte Tabelle ist mit den Wissensräumen in Phase B4 gefallen (Migration 163);
was bleibt, ist die Frage dahinter, und die lautet jetzt „welche App sieht
welcher Mitarbeiter". Ohne `permission`-Spalte: eine App ist freigegeben oder
nicht, und wer innerhalb der App was darf, entscheidet die App. `app_id` ist
bis C3 ein freier Text in Slug-Form, weil es die Tabelle `apps` noch nicht
gibt; C3 setzt den Fremdschlüssel nach.

## Was die Abnahme misst

`scripts/test/mitarbeiter-abnahme.sh` gegen `https://localhost:8443` läuft den
ganzen Lebenslauf eines Mitarbeiters in einem Durchgang: anlegen, mit E-Mail
anmelden, App freigeben, Freigabe sehen (mit Namen, nicht nur mit Nummer),
Passwort vom Administrator setzen lassen, eigenes Passwort wechseln,
stilllegen, wieder zulassen, Freigabe zurücknehmen, löschen. Dazwischen steht
jedes Mal die Gegenprobe: acht Verwaltungswege mit dem Mitarbeiter-Token, jeder
403, und der Administrator, der sich nicht selbst stilllegen kann.

Das Skript räumt seinen Benutzer auch dann weg, wenn unterwegs etwas rot war
(`trap ... EXIT`). Es braucht **sechs** Anmeldungen, die Drossel erlaubt zehn
je Viertelstunde und IP; zwei Läufe hintereinander sprengen sie, und
`rollen-abnahme.sh` (drei Anmeldungen) direkt davor ebenfalls.

## Rollenverteilung am Kopf des Branches

| Schutz                    | Routen | Differenz zu C1     |
| ------------------------- | ------ | ------------------- |
| nur `admin`               | 150    | +5 (die neuen Wege) |
| `admin` und `mitarbeiter` | 19     | unverändert         |
| API-Schlüssel             | 10     | unverändert         |
| öffentlich mit Grund      | 9      | unverändert         |
| **Gesamt**                | 188    | +5                  |

## Prüfung am Kopf des Branches

`run-tests.sh --quality` PASSED. `run-tests.sh --backend` ALL PASSED:
Selbsttest der Wächter bestanden, Rollenregeln 188 Routen, Endpunkte 187 ohne
Lücke, Werksreset 56 Tabellen alle eingeordnet, Jest 87 Dateien, 1 465 Tests
grün (davon 23 für `/api/benutzer` und 12 für `/api/freigaben`).

Nicht geprüft am Gerät: die neuen Wege antworten dort noch mit 404, weil der
Orin den Stand von C1 fährt (am 27.08.2026 über den Tunnel nachgesehen:
`GET /api/freigaben` → 404, `PUT /api/benutzer/17/aktiv` → 404, `GET
/api/benutzer` → 200). Der Befehl für nach dem Deploy steht im PR-Text.

## Der Wackler in der Rollen-Abnahme

Aufgabe war, ihn zu benennen, wenn er sich findet: ein Fall von
`rollen-abnahme.sh` war beim ersten Lauf rot und beim zweiten grün, ohne dass
festgehalten wurde, welcher.

Nachstellen ließ er sich nicht. Benennen schon, und zwar an der Stelle, die ein
solches Verhalten überhaupt erst unlesbar macht: `hole_token` gab **nur den
Token** zurück. War er leer, meldete die Abnahme `ROT  Abnahme-Admin meldet
sich an` ohne jede Angabe und brach mit „Ohne beide Sitzungen gibt es nichts zu
messen" ab. Ein 429 der Anmeldedrossel, ein 401 bei falschem Passwort und ein
403 bei stillgelegtem Konto sahen dabei identisch aus.

Die wahrscheinlichste Ursache passt genau auf „erst rot, dann grün":
`loginLimiter` erlaubt **zehn** Anmeldungen je Viertelstunde und IP, die
Rollen-Abnahme braucht drei. Wer sich vorher im Browser ein paarmal angemeldet
hat, hat den Rest schon verbraucht; der zweite Lauf eine Viertelstunde später
trifft ein leeres Fenster. Das erklärt auch, warum es der einzige rote Fall war
und warum er ohne Namen blieb. Ein Beweis ist es nicht, und als solcher steht
er hier auch nicht.

Zwei Nachbesserungen an `rollen-abnahme.sh`. Beide messen dasselbe wie vorher,
nur nachvollziehbar:

1. `hole_token` legt den HTTP-Code ab, und jede der drei Anmelde-Prüfungen
   zeigt ihn. Ein 429 heißt jetzt 429.
2. `rufe` wiederholt nicht nur bei 429, sondern auch bei `000` (curl-Zeitlimit
   oder abgebrochene Verbindung). `000` galt bisher als endgültige Antwort, und
   eine Box, auf der nebenher ein Flow die GPU hält, braucht für eine
   Sammel-Auskunft schon mal länger als die zwanzig Sekunden. Das Zeitlimit
   steht jetzt auf dreißig.

Der Code landet dabei in einer **Datei**, nicht in einer Variablen:
`TOK=$(hole_token ...)` ist eine Kommandosubstitution und damit eine Subshell,
in der eine gesetzte Variable beim nächsten Befehl wieder weg ist. Der erste
Entwurf von `mitarbeiter-abnahme.sh` machte genau diesen Fehler und meldete
`HTTP ` ohne Zahl; gegen das Gerät gefahren fiel es sofort auf.

## Der Fehler, den das Review gefunden hat

`'1' === 1` ist false, und daran hingen zwei Schutzwälle.

`admin_users.id` ist `BIGSERIAL`, also `int8`. `node-postgres` liefert `int8`
als **Zeichenkette**, weil eine 64-Bit-Zahl nicht in eine JS-Zahl passt, und
dieses Repo setzt keinen `pg.types.setTypeParser` dagegen (`grep -r
setTypeParser src/` findet nichts). `req.user.id` aus `requireAuth` ist also
`'1'`. `req.params.id` kommt dagegen durch `z.coerce.number()` und ist die Zahl
`1`. Am Gerät nachgesehen am 27.08.2026: `GET /api/auth/me` liefert
`"id": "1"`, mit Anführungszeichen.

Betroffen waren beide Selbstschutz-Prüfungen in `routes/admin/benutzer.js`:

- `PUT /:id/aktiv` — „das eigene Konto kann nicht stillgelegt werden" (neu in
  dieser Phase),
- `DELETE /:id` — „das eigene Konto wird über `/gdpr/me` gelöscht" (aus C1,
  seit dem 27.08.2026 auf `main`).

Keine der beiden hat je gegriffen. Für die Löschung war das folgenlos, weil
`/gdpr/me` dieselbe Löschung ausführt; für die Stilllegung nicht: sobald zwei
Administratoren existieren, sperrt der Handelnde sich selbst aus, und
`blacklistAllUserTokens` beendet seine Sitzung dabei sofort. Der
Letzte-Admin-Schutz fängt nur das Ein-Zugang-Gerät ab.

Gefixt an beiden Stellen über einen benannten Baustein `istEigenesKonto(req)`,
der über `Number` vergleicht und die Falle im Kommentar trägt.

**Warum die Tests es nicht gemerkt haben**, und das ist der eigentliche Befund:
die Mocks setzten `req.user` als `{ id: 1, ... }`, also mit einer Zahl. Ein
Mock, der etwas anderes liefert als die Datenbank, prüft den Code gegen eine
Welt, die es nicht gibt. Die Kennungen in `benutzer.test.js` und
`freigaben.test.js` sind deshalb jetzt Zeichenketten, so wie `pg` sie liefert.
Gegenprobe gemacht: mit dem alten Vergleich fallen drei Tests um, mit dem neuen
sind es 23 grüne.

Die Live-Abnahme hätte den Fall ebenfalls gefunden (Abschnitt 8, „Der
Administrator kann sich nicht selbst stilllegen") — nur eben erst nach dem
Deploy.

## Entscheidungen unterwegs

| Entscheidung                                      | Warum                                                                                                                                                                                                                                                                      |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Passwort setzen ohne Komplexitätsregeln           | Dieselbe Entscheidung wie beim Anlegen in C1: der Administrator vergibt ein Startpasswort, das der Mitarbeiter wechselt, und dort greifen die Regeln. Strenger auf dem Setz-Weg als auf dem Anlege-Weg wäre eine Hürde ohne Wirkung.                                       |
| Ein Schreibweg für beide Passwortwechsel          | `schreibePasswort` in `passwordService.js`; zwei Schreibwege wären zwei Gelegenheiten, `password_history` zu vergessen.                                                                                                                                                    |
| `PUT /:id/aktiv` statt „sperren" und „entsperren" | Ein Wert, zwei Richtungen. Zwei Endpunkte wären zwei Wege zu demselben Feld.                                                                                                                                                                                               |
| Kein `permission` in `app_members`                | 089 hatte drei Stufen für einen Wissensraum. Eine Spalte, die niemand liest, ist eine Zusage, die niemand einhält.                                                                                                                                                         |
| Zweite Freigabe ist 200, nicht 409                | Zweimal derselbe Zustand ist kein Konflikt. Der erste Zeitstempel und der erste Administrator bleiben stehen, `neu: false` sagt es dem Aufrufer.                                                                                                                           |
| Freigabe auch an einen Administrator              | Die Rolle sagt, wer verwaltet, nicht wer arbeitet. Eine Sonderregel „Admins sehen alles" wäre eine zweite Wahrheit neben der Tabelle.                                                                                                                                      |
| Stilllegen beendet Sitzungen UND leert den Cache  | `requireAuth` prüft `is_active` auf dem zwischengespeicherten Stand (60 s TTL). Ohne beides wäre die Stilllegung erst eine Minute später wirksam.                                                                                                                          |
| Freigaben in der DSGVO-Auskunft                   | Welche Apps jemandem freigegeben sind, ist ein personenbezogenes Datum über ihn. In der Löschung stehen sie ausdrücklich, obwohl `ON DELETE CASCADE` sie ohnehin mitnimmt: eine Zusammenfassung, die eine Kategorie verschweigt, sieht aus wie eine, zu der es nichts gab. |
| Keine Drossel auf `PUT /:id/passwort`             | `passwordChangeLimiter` schützt vor dem Erraten des ALTEN Passworts. Hier gibt es nichts zu erraten, und ein Administrator, der zehn Mitarbeiter einrichtet, würde nur aufgehalten.                                                                                        |
