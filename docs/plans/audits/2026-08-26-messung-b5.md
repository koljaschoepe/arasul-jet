# Messung, Phase B5 des Überordner-Plans vom 26.08.2026

Backend-Rückbau 2: n8n-Container, n8n-Runner, n8n-Schema und der Service
dazu, SearXNG mit den Web-Werkzeugen der Flows, die Plattform-Apps
(Migration 100) und die Migration dazu, `compose/` bereinigt. Gezählt mit
`python3 scripts/test/zeilen.py` (die Messregel aus der Löschliste, siehe
[`2026-08-26-loeschliste-b1.md`](2026-08-26-loeschliste-b1.md), Frage 7).

## Vorher und nachher

Vorher ist der Stand `28348e36` auf `main` (Kopf von B4, Basis des Branches
`phase/b5`), nachher der Kopf des Branches.

| Bereich                                          | vorher  | nachher | Differenz   |
| ------------------------------------------------ | ------- | ------- | ----------- |
| Frontend `apps/dashboard-frontend/src`           | 32 759  | 31 295  | −1 464      |
| Backend-Routen `src/routes`                      | 11 204  | 10 556  | −648        |
| Backend-Services `src/services`                  | 23 760  | 22 371  | −1 389      |
| Backend übrig                                    | 6 789   | 6 707   | −82         |
| Backend-Tests `__tests__`                        | 28 436  | 26 770  | −1 666      |
| Dienste und Container (`services/*`, `config/*`) | 33 206  | 29 051  | **−4 155**  |
| Skripte (`scripts/*`)                            | 24 757  | 23 188  | −1 569      |
| **Gesamt**                                       | 160 911 | 149 938 | **−10 973** |

Die Zeilen der Messregel sind nur die halbe Wahrheit dieser Phase: `git diff
--stat origin/main` zählt 252 Dateien, +916 und −36 141 Zeilen. Der Rest liegt
außerhalb der gemessenen Ordner: die drei `package-lock.json` der n8n-Custom-
Nodes (22 000 Zeilen), die n8n-Vorlagen und -Workflows, die n8n-eigenen
Dokus (`docs/integrations/N8N*.md`, `CUSTOMER_OAUTH_SETUP.md`,
`DATENSCHUTZ_N8N.md`, `N8N_LIZENZ.md`, `DRITTLAND_KONNEKTOREN.md`,
`.claude/context/n8n-workflow.md`) und die Abschnitte in API-Referenz,
Schema, Umgebungsvariablen, Architektur, Infrastruktur, Backup und
Admin-Handbuch.

Die Löschliste hatte für B5 im Backend `workflows.js`, `automations.js` und
`workspaceApps.js` (423 Zeilen, 10 Endpunkte) und `services/n8n/` (4 047
Zeilen) als „fällt" gezählt. Gefallen ist mehr, weil die Kopplung an n8n
und SearXNG tiefer saß als drei Routen: `n8nLogger`, `appLifecycleService`
(glich den n8n-Container an den Schalter in `platform_apps` an), die Web-
Werkzeuge `web_suche`/`web_lesen` samt `tools/web.js`, `POST /api/apps/sync`,
`GET /api/apps/:id/n8n-credentials`, `POST /api/events/webhook/n8n`,
`POST /api/settings/password/n8n`, im Werksreset das Leeren und Neuanlegen
des Schemas `n8n` und der Container-Neustart, im Bootstrap Schritt 4. Im
Frontend der Automationen-Tab (iframe auf `/n8n/`), `n8nDesign.ts` (das
Theming des eingebetteten Editors), die n8n-Anleitung in „Allgemein", der
n8n-Hinweis in der Passwortverwaltung und `useWorkspaceApps` mit dem
Kern-App-Gating in ActivityBar und Shell; der Workspace-Store steigt auf v9
und wirft den Tab `automationen` aus alten Ständen.

## Die Routen

Der Endpunkt-Zähler der Löschliste (`router.<verb>(`-Aufrufe unter
`src/routes`): **222 in 32 Dateien vorher, 206 in 29 Dateien nachher.**
Gefallen sind `/api/workflows` (7), `/api/automations/session` (1),
`/api/workspace-apps` (2), `POST /api/apps/sync`, `GET /api/apps/:id/n8n-credentials`,
`POST /api/events/webhook/n8n`, `POST /api/settings/password/n8n` und
`GET /api/flows/beispiele` samt `/:name` (2): der Beispiel-Katalog der Flows
hatte nach B4 nur noch `recherche`, und das brauchte genau die Web-Werkzeuge,
die mit SearXNG gehen; ein Katalog ohne Beispiel ist toter Code
(`beispielKatalog.js`, zwei Tests, der Eintrag in `endpunkte-live.py`).

## Die Tabellen

Migration `164_rueckbau_b5.sql` entfernt das Schema `n8n` (111 Tabellen, die
n8n selbst angelegt hat und die in keiner Migration stehen), die vier
Tabellen der Kopplung (`workflow_activity`, `n8n_external_call_log`,
`n8n_allowed_external_domains`, `arasul.n8n_audit_log`) mit drei
Trigger-Funktionen aus 090, und `arasul.platform_apps` (100, 108, 108a).
Die Werksreset-Liste (`services/werksreset/tabellen.js`) kennt 58 statt 63
Tabellen; der Wächter `werksreset-tabellen.py` ist grün. Ob die Migration am
Orin läuft, belegt der Deploy; die Befehle dafür stehen im PR-Text.

**Vor dem Löschen gesichert** (26.08.2026 20:16, vor jeder Änderung am
Gerät): `/home/arasul/sicherungen/b5-n8n-2026-08-26/` mit `n8n-schema.sql.gz`
(Schema `n8n`, 111 Tabellen, 2 Workflows), `n8n-tabellen.sql.gz` (die fünf
Tabellen oben), `n8n-volume.tar.gz` (Volume `arasul-platform_arasul-n8n`),
`n8n_encryption_key` und `SHA256SUMS`. Zurückspielen: `zcat n8n-schema.sql.gz
| docker exec -i postgres-db psql -U arasul arasul_db` (dito
`n8n-tabellen.sql.gz`), Schlüssel nach `config/secrets/`, dann die
Compose-Dienste aus dem Stand `28348e36` wieder anlegen.

## Die Container

`compose/*.yaml` definieren 12 Dienste statt 15: `n8n`, `n8n-runners` und
`searxng` sind weg, mit ihnen 52 Zeilen Secrets (`n8n_encryption_key`,
`n8n_runners_auth_token`, `n8n_owner_email`, `n8n_owner_password`), das
Volume `arasul-n8n`, `n8n-agent-workspace`, die Traefik-Router `n8n`,
`n8n-healthz`, `n8n-favicon`, `n8n-webhooks`, der WebSocket-Router und die
Middleware `basicAuth-n8n`. `deploy-local.sh` räumt mit `--remove-orphans`
ab, sonst liefen die drei am Gerät als Waisen weiter. `cloudflared` bleibt
(Profil `tunnel`, Fernzugriff), seine Ingress-Vorlage zeigt auf das Dashboard
statt auf n8n-Webhooks.

Am Orin liefen vor der Phase 16 Container (`docker ps -q | wc -l`; 14 aus dem
Compose-Projekt, dazu `jetcam` und `arasul-flows-sandbox`). Erwartet nach dem
Deploy: 13 (11 + 2). Die Zählung nachher macht der Überordner.

## Was über die Liste hinaus gefallen ist, und warum

| Was                                                                                                                            | Warum                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Web-Werkzeuge `web_suche`, `web_lesen`, Beispiel `recherche`, der Beispiel-Katalog                                             | Einziger Nutzer von SearXNG; das letzte Beispiel brauchte sie. B4 hatte „recherche bleibt" gesagt, das galt mit SearXNG.                                                                                                                               |
| RAM-Überlast in der Selbstheilung nur noch als Meldung                                                                         | Der einzige Hebel war, n8n anzuhalten und nach drei ruhigen Zyklen wieder zu starten (`pause_n8n_workflows`, `resume_n8n_workflows`). Ohne n8n gibt es keinen Dienst, den man gefahrlos anhält; Ollama gibt sein Modell nach `KEEP_ALIVE` selbst frei. |
| Neustart-Sicherheitscheck 5 der Selbstheilung (`workflow_activity`)                                                            | Die Tabelle fällt mit 164.                                                                                                                                                                                                                             |
| Schlüssel-Escrow im `backup-service`, `restore.sh --n8n`, n8n-Export in `backup.sh`                                            | Sicherten den n8n-Schlüssel und die Workflows; ohne n8n sichern sie nichts.                                                                                                                                                                            |
| `scripts/ops/n8n-auto-update.sh`, `n8n-import-templates.sh`, drei OAuth-Tunnel-Skripte, `external-integrations-smoke/-soak.sh` | Die Löschliste führt sie unter „Abnahmeskripte, die fallen"; alle riefen n8n.                                                                                                                                                                          |
| Neun Einträge in `durchreichung.py`, `ERLAUBTE_LUECKE`                                                                         | `DOCKER_GATEWAY_IP`, `N8N_*` (3), `SEARXNG_URL`, `SSH_PORT`, `SSH_USER`, `WEB_*_TIMEOUT_MS` (2): weder dokumentiert noch gelesen, der Wächter selbst hat sie als streichbar gemeldet.                                                                  |
| `docs/legal/DRITTLAND_KONNEKTOREN.md`, `DATENSCHUTZ_N8N.md`, `N8N_LIZENZ.md`                                                   | Alle drei beschrieben n8n-Konnektoren, n8n-Datenschutz und die n8n-Lizenz. Der AVV-Entwurf nennt jetzt Fremdsysteme über die externe API. G5 Recht bleibt bei Kolja; ob der AVV-Text so trägt, steht im PR unter „Offen".                              |
| `restore-drill.sh`, `KUNDENTABELLEN` ohne `arasul.extensions`, `extension_zeitplaene`                                          | Beide Tabellen sind seit 163 (B4) weg; die Liste stand daneben und ist im selben Block bereinigt. `CRITICAL_TABLES` (mit `documents`, `document_chunks`) ist C9 und bleibt: der Drill entscheidet nach dem Abzug, nicht nach der Liste.                |
| Aus B4 offen: `FRONTEND_HANDBOOK.md`                                                                                           | Nannte `tabs`, `separator`, `skeleton`, `dropdown-menu`, `tooltip`, `table` als shadcn-Bausteine; es gibt dreizehn andere. Die Liste zeigt jetzt, was liegt.                                                                                           |

Geschichte bleibt Geschichte: wo n8n erklärt, warum ein Wert so steht (der
Nachlauf der Wartung in `config.py` der Selbstheilung, die Cookie-Lebensdauer,
der Verbindungsabriss vom 23.08.2026 in `category_handlers.py`), bleibt es als
Datum genannt. Die Tests dazu prüfen dasselbe Verhalten jetzt an
`dashboard-backend` als Beispielcontainer.

## Prüfung am Kopf des Branches

Backend: Jest 89 Dateien grün (vorher 96), 1 667 Tests, ESLint 0 Fehler.
Frontend: `tsc` 0, ESLint 0, Vitest 625/625 in 65 Dateien (vorher 648 in
69), `knip` ohne Befund. Selbstheilung: pytest 36/36 (vorher 40; die vier
n8n-Pause-Tests sind weg). Wächter: `run-tests.sh --quality` (toter Code,
Gedankenstriche, Bausteine, Namensregister, Einheiten, Durchreichung,
Datenordner, Werksreset-Tabellen, Pfadfilter, Routenregeln, stiller Tod,
Endpunkte, Anleitungen, Design-System, Code-Qualität) grün; `endpunkte` 201
Endpunkte, 0 Lücken; `durchreichung` 48 bekannte Lücken, keine neue, kein
streichbarer Eintrag mehr; `anleitungen` keine Befunde. bats für
`interactive-setup` und `detect-platform`: 61 von 63 grün, die zwei roten
lesen `/proc/meminfo` und `nproc` und sind auf macOS immer rot, auch vorher.
`tests/unit` (ohne CI-Job) lässt sich hier nicht sammeln (`requests` fehlt
im lokalen Python), die Änderung darin ist ein umbenannter Beispielcontainer
und ein gestrichener n8n-Test. `docker compose config` konnte in dieser
Sitzung nicht laufen (kein Docker); die Compose-Dateien wurden mit PyYAML
geparst, 12 Dienste, alle `depends_on`, `secrets`, `volumes`, `networks`
aufgelöst, kein Verweis auf n8n oder SearXNG.
