# Messung, Phase B4 des Überordner-Plans vom 26.08.2026

Backend-Rückbau 1: Routen und Dienste der gestrichenen Bereiche (Sandbox,
Terminal-WebSocket, Dokumente, Wissensräume, Erweiterungs-Baukasten mit
Brücke, Memory-Kompaktion), die Migration dazu, und nach den Entscheidungen
vom 26.08.2026 MinIO, Loki, Promtail und der Settings-Bereich für externe
Modelle. Gezählt mit `python3 scripts/test/zeilen.py` (die Messregel aus der
Löschliste, siehe [`2026-08-26-loeschliste-b1.md`](2026-08-26-loeschliste-b1.md),
Frage 7).

## Vorher und nachher

Vorher ist der Stand `8b7f7f89` auf `main` (Kopf von B3, Basis des Branches
`phase/b4`), nachher der Kopf des Branches.

| Bereich                                          | vorher  | nachher | Differenz   |
| ------------------------------------------------ | ------- | ------- | ----------- |
| Frontend `apps/dashboard-frontend/src`           | 33 331  | 32 759  | −572        |
| Backend-Routen `src/routes`                      | 17 437  | 11 204  | **−6 233**  |
| Backend-Services `src/services`                  | 41 718  | 23 760  | **−17 958** |
| Backend übrig                                    | 8 133   | 6 789   | −1 344      |
| Backend-Tests `__tests__`                        | 42 586  | 28 436  | **−14 150** |
| Dienste und Container (`services/*`, `config/*`) | 43 291  | 33 280  | **−10 011** |
| Skripte (`scripts/*`)                            | 27 267  | 24 757  | −2 510      |
| **Gesamt**                                       | 213 763 | 160 985 | **−52 778** |

Die Löschliste hatte für das Backend 39 066 Zeilen als „fällt" gezählt
(Routen 7 211, Services 15 547, Rest 1 020, Tests 16 288, ohne den
Indexer); B4 nimmt 39 685 aus dem Backend, weil der Chat-Agent, die
Projekte, Git und der Rechnungs-Flow mitgefallen sind (Begründung unten)
und der Wächter `toter-code.sh` danach drei Hilfsdateien ohne Importeur
fand (`fileValidation`, `slugGenerator`, `contentDisposition`, samt Test).
Der Frontend-Abzug ist der Settings-Bereich für externe Modelle, der
Dienst-Selektor der Passwortverwaltung und der dadurch verwaiste
Baustein `switch.tsx`. Bei den Diensten sind es der Indexer (9 797 auf
2 022), das Sandbox-Image (1 930), Loki, Promtail, die Doku dazu und der
Traefik-Router `sandbox-terminal-websocket`. Bei den Skripten neben Backup
und Compose-Helfern der Sandbox-Image-Bau in `deploy-local.sh` und
`create-factory-image.sh` (bauten aus `services/sandbox/`, das es nicht
mehr gab) und der Aufruf des mit 19188311 gelöschten `geruest-regeln.py`
in `run-tests.sh`.

## Die Routen

Der Endpunkt-Zähler der Löschliste (`router.<verb>(`-Aufrufe unter
`src/routes`): **373 in 45 Dateien vorher, 222 in 32 Dateien nachher.** Die
Liste hatte 173 als „fällt" gezählt; B4 nimmt 151, weil `workflows.js`,
`automations.js` und `workspaceApps.js` (n8n, Plattform-Apps, 10 Endpunkte)
zu B5 gehören und `chats.js`, `llm.js`, `documentImages.js` (Chat, 24) zu
B6 – mit Ausnahme von `documentImages.js`, das mit den Dokumenten fiel.

Gefallen sind `/api/sandbox` (28), `/api/claude-terminal` (5),
`/api/extensions` (27), `/api/documents` (19 + 2 Bilder), `/api/document-analysis`
(2), `/api/rag` (2), `/api/spaces` (16), `/api/projects` (23),
`/api/knowledge-graph` (8), `/api/memory` (4), `/api/git` (5),
`/api/modelle-extern` (6), dazu `POST /api/settings/password/minio`,
`GET/PUT /api/settings/company-context` und `GET /api/flows/sammlungen`.

## Die Tabellen

Migration `163_rueckbau_b4.sql` entfernt 30 Tabellen (die Löschliste
nannte 37; `workflow_activity`, `n8n_external_call_log`,
`n8n_allowed_external_domains`, `arasul.n8n_audit_log`,
`arasul.platform_apps` sind B5, `chat_attachments` ist B6, `bot_audit_log`
ist ein Telegram-Rest außerhalb der Phase), drei Spalten
(`flow_runs.projekt_id`, `system_settings.active_project_id`,
`system_settings.active_workspace_space_id`), zwei Sichten, 23 Funktionen,
sechs Aufzählungstypen und die Laufzeit-Schemata `ext_<slug>`.
`run_all_cleanups()` hat 15 statt 20 Aufrufe. Die Werksreset-Liste
(`services/werksreset/tabellen.js`) kennt 63 statt 93 Tabellen; der
Wächter `werksreset-tabellen.py` ist grün. Ob die Migration am Orin läuft,
belegt der Deploy; die Befehle dafür stehen im PR-Text.

## Was über die Liste hinaus gefallen ist, und warum

| Was                                                                                                                                                 | Warum                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Projekte (`/api/projects`, `services/projects`, `rag/`), Git                                                                                        | Das Ein-Ordner-Modell ist ein Wissensraum mit Sandbox-Mount (Migration 129); ohne beides bleibt ein Ordner ohne Leser. Die Löschliste führt sie ohnehin als „fällt", nur ohne Phase.                                                                  |
| Chat-Agent (`chatAgentRunner`, `agentTodoTool`)                                                                                                     | Die Werkzeugschleife über Projektablage, Wissensraum und Sandbox; seit B2 ruft ihn keine Oberfläche mehr. `chats.js`/`llm.js` bleiben bis B6, ohne `agent`, `ablage_ziel`, `space_ids`, ohne Kontextverdichtung.                                      |
| Rechnungs-Flow (`flows/rechnung`)                                                                                                                   | Schreibt in die Projektablage und den Nummernkreis je Projekt.                                                                                                                                                                                        |
| Flow-Argumenttypen `datei`, `wissensbasis`, `ordner`; Werkzeuge `terminal`, `rag_suche`, `rechnung_erstellen`; `projekt://`; projektgebundene Flows | Jeder davon zeigte auf Dokumente, Wissensräume, Projekte oder die Sandbox. Ordner eines Flows sind jetzt genau die deklarierten. Die Beispiele `angebot`, `dokument-zusammenfassen`, `wissen` brauchten diese Typen und sind weg; `recherche` bleibt. |
| Systemprompt nur noch Basis                                                                                                                         | KI-Profil hing an Memory, Unternehmenskontext an `company_context`.                                                                                                                                                                                   |
| `gpuVorrang.js`                                                                                                                                     | Der Indexer rechnet nicht mehr; niemand, dem der Lock eine Frist melden müsste.                                                                                                                                                                       |
| Indexer auf `/health` + `/extract-text`                                                                                                             | Alles andere las die gefallenen Tabellen oder MinIO. Nebenbei: `extract_metadata` wurde mit zwei statt drei Argumenten gerufen, `pages`/`title` kamen nie an.                                                                                         |

Geblieben, obwohl ohne Aufrufer bis D4: `services/llm/extern` samt
`schluesselSetzen`, `schluesselEntfernen`, `aktivSetzen`,
`schluesselPruefen`, `anbieterStand` (Tests decken sie), Migration 153, wie
Kolja es entschieden hat. `medienService` bleibt: er ist der USB-Export der
DSGVO-Auskunft, nicht „Bilder im Chat", wie die Löschliste vermutete.

## Prüfung am Kopf des Branches

Backend: Jest 96 Dateien grün (vorher 132), 1 794 Tests, ESLint 0 Fehler.
Frontend: `tsc` 0, ESLint 0, Vitest 648/648, `knip` ohne Befund. Indexer:
pytest 24/24. Selbstheilung: pytest 40/40. Wächter: `run-tests.sh
--quality` (toter Code, Gedankenstriche, Bausteine, Namensregister,
Einheiten, Durchreichung, Datenordner, Werksreset-Tabellen, Pfadfilter,
Routenregeln, stiller Tod, Endpunkte, Anleitungen, Design-System,
Code-Qualität) grün; `endpunkte` 217 Endpunkte, 0 Lücken; `durchreichung`
57 bekannte Lücken, keine neue (die 30 Stellschrauben der gestrichenen
Bereiche sind aus der Ausnahmeliste raus); `anleitungen` keine Befunde;
`zeilen.py --selbsttest` und `waechter-selbsttest.sh` bestanden. Die
Rollback-Meldung prüft der Runner auf macOS nicht (bash 3.2), sie läuft in
der CI. `docker compose config` konnte in dieser Sitzung nicht laufen
(kein Docker); die Compose-Dateien wurden mit PyYAML geparst und ihre
Verweise (depends_on, secrets, volumes, networks) gegengeprüft.
