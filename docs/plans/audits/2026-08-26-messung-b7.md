# Messung, Phase B7 des Überordner-Plans vom 26.08.2026

Aufräumen: `docs/` ohne Beschreibungen gestrichener Dinge, `CLAUDE.md` und
`README.md` auf das Zielbild, `LICENSE` auf „einsehbar, Nutzung nur mit
Lizenz", Versionsnummer ehrlich, Design-Dokument angelegt, dazu die aus B6
offene Migration. Gezählt mit `python3 scripts/test/zeilen.py` (die Messregel
aus der Löschliste, siehe
[`2026-08-26-loeschliste-b1.md`](2026-08-26-loeschliste-b1.md), Frage 7).

## Vorher und nachher

Vorher ist der Stand `c0429e32` auf `main` (Kopf von B6, Basis des Branches
`phase/b7`), nachher der Kopf des Branches.

| Bereich                                          | vorher  | nachher | Differenz |
| ------------------------------------------------ | ------- | ------- | --------- |
| Frontend `apps/dashboard-frontend/src`           | 31 293  | 31 293  | 0         |
| Backend-Routen `src/routes`                      | 9 300   | 9 300   | 0         |
| Backend-Services `src/services`                  | 21 484  | 21 484  | 0         |
| Backend übrig                                    | 6 565   | 6 566   | +1        |
| Backend-Tests `__tests__`                        | 23 660  | 23 660  | 0         |
| Dienste und Container (`services/*`, `config/*`) | 29 182  | 29 213  | +31       |
| Skripte (`scripts/*`)                            | 22 336  | 22 336  | 0         |
| **Gesamt**                                       | 143 820 | 143 852 | **+32**   |

Die Messregel zählt Code, keine Doku; diese Phase ist eine Doku-Phase. Was sie
im Code ändert, sind Migration 166 (+31 Zeilen unter `services/`) und ein
Kommentar in `utils/version.js` (+1). Die Doku selbst, mit `wc -l` über
`docs/` ohne `docs/plans/`, dazu `README.md`, `CLAUDE.md`, `CONTRIBUTING.md`,
`apps/*/CLAUDE.md`, `.claude/context/*.md` und `LICENSE`: **18 360 → 17 048 Zeilen (−1 312)**. `git diff --stat origin/main` am Kopf: 52 Dateien, +709,
−1 887.

## Was gefallen ist

- **`docs/development/DESIGN_SYSTEM.md`** (1 196 Zeilen, mit einer
  Kopiervorlage aller CSS-Variablen, die `index.css` ohnehin hält). Ersetzt
  durch [`docs/development/DESIGN.md`](../../development/DESIGN.md), 120
  Zeilen: Grundsatz und die drei Regeln mit ihren Wächtern, Stack, die
  Jet-Palette in drei Themes, Maße, das Baustein-Set, die Shell, wo was
  steht. Alle vierzehn Verweise (CLAUDE.md, INDEX, DEVELOPMENT, ONBOARDING,
  Frontend-README und -CLAUDE.md, zwei Bausteine, ein Test,
  `check-design-system.js`) zeigen auf die neue Datei.
- **`docs/features/WORKSPACE.md`** (32 Zeilen, seit B5 nur noch eine Liste
  dessen, was es nicht mehr gibt). `anleitungen.py` prüft an ihrer Stelle
  `docs/features/FLOWS.md`.
- **Vier Kapitel des Admin-Handbuchs** (Chat, Dokumente & RAG, Workspace,
  Automation), die nur noch sagten, dass es das Beschriebene nicht mehr gibt;
  aus zwölf Kapiteln werden acht.
- **Die Absätze „gibt es nicht mehr"** in CLAUDE.md, README, ARCHITECTURE,
  DATABASE_DOMAINS, API_REFERENCE, ENVIRONMENT_VARIABLES, FLOWS, DEPLOYMENT,
  INFRASTRUCTURE, BACKUP_SYSTEM, DEVELOPMENT, ONBOARDING, CONTRIBUTING und in
  den sechs Kontextpaketen unter `.claude/context/`. Was blieb, ist je Datei
  ein Satz über den heutigen Stand, kein Abriss der Phasen B2 bis B6; die
  Geschichte steht in den Messungen dieses Ordners.
- **Beispiele mit gestrichenen Werkzeugen**: der `recherche`-Flow mit
  `web_suche`/`web_lesen` in FLOWS.md und API_REFERENCE wird ein
  `zusammenfassung`-Flow mit `dateien_lesen`/`dateien_suchen`; ein Flow mit
  den Web-Werkzeugen wird seit B5 vom Schema abgewiesen, die Doku empfahl ihn
  noch. TROUBLESHOOTING 5 („Dokumente werden nicht indexiert") wird
  „Text-Extraktion schlägt fehl". QUICK_START und FRESH_INSTALL_CHECKLIST
  prüfen Modelle und die externe API statt Chat und Upload. Die AVV-Vorlage
  nennt Flow-Läufe statt Chat-Verläufe und RAG-Index.

## Was neu ist

- **`LICENSE`**: „Der Quelltext ist einsehbar, die Nutzung braucht eine
  Lizenz." Sechs Punkte: Einsehen erlaubt; Betreiben nur mit Lizenz;
  Weitergabe nur mit Erlaubnis; Beiträge räumen ein Nutzungsrecht ein; keine
  weiteren Rechte; keine Gewähr. Das Ara-Kit ist als Apache-2.0-Repo daneben
  benannt. Der alte Text („unauthorized copying strictly prohibited")
  widersprach dem öffentlichen Repo (Zeile 22 des Überordner-Plans).
- **Version ehrlich (altes J3)**: `versionFuerVergleich()` fällt auf `0.0.0`
  statt `1.0.0` zurück, `package.json` (Wurzel, Backend, Frontend) und das
  Lockfile sagen `0.0.0`. Nichts liest die `package.json`-Version; der
  Rückfall in `version.js` war die letzte Stelle, die 1.0.0 behauptete.
  Wirkung: `checkForUpdates` hält jede angebotene Fassung für neuer, was für
  eine Vorserie stimmt; `validateManifest` lehnt ein Paket mit `min_version`
  über 0.0.0 ab, bis `SYSTEM_VERSION` gesetzt ist. Die README-Behauptung, das
  Update vergleiche gegen `package.json`, war falsch und ist raus.
- **Migration 166**: `cleanup_old_metrics()` ohne `workflow_activity`. Seit
  164 die Tabelle fehlte, fing der `EXCEPTION WHEN OTHERS`-Block aus 001 den
  Fehler als NOTICE, und die drei DELETEs dahinter (`self_healing_events`,
  `system_snapshots`, `service_restarts`) liefen nicht mehr. Der Fänger fällt
  mit; `run_all_cleanups()` (165) meldet Fehler selbst in seinem JSON.
- **`docs/INDEX.md`** nennt jetzt wirklich jede Doku: DATABASE_DOMAINS,
  FRONTEND_HANDBOOK, PYTHON_SERVICES, CICD, INFRASTRUCTURE,
  FRESH_INSTALL_CHECKLIST und `legal/` fehlten.

## Prüfung am Kopf des Branches

`run-tests.sh --quality` ALL PASSED (toter Code keiner, Gedankenstriche,
Bausteine, Namensregister, Einheiten, Durchreichung, Datenordner,
Werksreset-Tabellen, Pfadfilter, Routenregeln, stiller Tod, Endpunkte 179 ohne
Lücke, Anleitungen: 4 Dateien, keine Befunde, Faden, Selbsttest, Design-System,
Code-Qualität). Backend Jest 84 Dateien, 1 424 Tests grün. Frontend `tsc` 0,
Vitest 65 Dateien, 625/625. Prettier über alle geänderten Markdown-Dateien.

## Entscheidungen unterwegs

| Entscheidung                                             | Warum                                                                                                                                                                                                             |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DESIGN_SYSTEM.md ersetzt, nicht ergänzt                  | Zwei Design-Dokumente wären doppelte Bausteine; die Werte stehen in `index.css`, die Kopiervorlage war eine zweite Quelle, die driftet.                                                                           |
| Vergleichszahl 0.0.0, nicht `SYSTEM_VERSION` beim Deploy | Ein per `deploy-local.sh` exportierter Wert gälte nur für diesen `compose up`; ein `./arasul restart` von Hand fiele auf „Vorserie" zurück. Die Zahl kommt aus `.env` oder dem Bau, beides nicht in dieser Phase. |
| AVV-Vorlage angefasst                                    | Sie beschrieb Chat-Verläufe und einen RAG-Index als verarbeitete Daten. G5 Recht bleibt bei Kolja; geändert sind nur die zwei Aufzählungen, kein Vertragstext.                                                    |
| `bot_audit_log` bleibt in DATABASE_DOMAINS               | Die Tabelle existiert (Werksreset-Liste); sie ist als leer und ohne Schreiber markiert. Sie zu löschen ist eine Migration, keine Doku.                                                                            |
