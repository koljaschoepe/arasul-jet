# =============================================================================
# Arasul Platform - Environment Variables Template
# Generated by: ./arasul setup (interactive_setup.sh)
# Documentation: docs/ENVIRONMENT_VARIABLES.md
# =============================================================================

# --- System ---
SYSTEM_NAME=arasul
# SYSTEM_VERSION bleibt bewusst UNGESETZT, solange die Auslieferung nicht
# versioniert ist. Ungesetzt liest ein Mensch ueberall "Vorserie" statt einer
# fertigen 1.0.0, die es nicht gibt (Plan 023 C6, Befund F-19). Sobald es
# Releases gibt, setzt der Bau hier die Nummer aus dem Tag.
# SYSTEM_VERSION=1.2.0
NODE_ENV=production

# --- Administrator (set during setup) ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=<generated-during-setup>
ADMIN_EMAIL=admin@example.com

# --- Security (auto-generated, DO NOT use weak values) ---
# JWT_SECRET: min 32 chars, generated with: openssl rand -hex 32
JWT_SECRET=<generated-during-setup>

# --- Database ---
POSTGRES_USER=arasul
POSTGRES_PASSWORD=<generated-during-setup>
POSTGRES_DB=arasul_db
POSTGRES_HOST=postgres-db
POSTGRES_PORT=5432

# --- AI Model ---
# Nur Modelle aus der Kurzliste (Phase C8, config/modelle/kurzliste.json).
# Hier stand bis zum 28.08.2026 `qwen3:14b`, ein Modell, das es im Katalog
# nicht mehr gibt. Der Werksreset zog es trotzdem, dreissig Minuten lang.
# `scripts/test/kurzliste.py` haelt diese Zeile jetzt an der Kurzliste.
LLM_MODEL=gemma4:e4b
LLM_SERVICE_HOST=llm-service
LLM_SERVICE_PORT=11434

# --- Embedding ---
EMBEDDING_SERVICE_HOST=embedding-service
EMBEDDING_SERVICE_PORT=11435
EMBEDDING_MODEL=BAAI/bge-m3

# --- Dashboard ---
DASHBOARD_BACKEND_PORT=3001
JWT_EXPIRY=24h
LOG_LEVEL=info
