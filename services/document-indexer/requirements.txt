# Document Indexer: Textextraktion aus Uploads (Phase B4, 26.08.2026).
# Nur was /extract-text braucht. Kein MinIO, kein Postgres, kein Embedding.

# Dokument-Parser
PyMuPDF==1.28.2
pdfplumber==0.11.0
python-docx==1.2.0
PyYAML==6.0.3

# OCR fuer Bild-PDFs und Bilddateien (lokales Tesseract im Image)
pdf2image==1.17.0
Pillow==10.2.0
pytesseract==0.3.13

# HTTP-API
flask==3.1.3
flask-cors==6.0.5
