# Nur was die Tests wirklich brauchen. Die schweren Parser-Abhaengigkeiten
# (PyMuPDF, pdfplumber, python-docx, pytesseract) stubben die Tests selbst,
# siehe tests/conftest.py.
#
# MUSS zu den Versionen in requirements.txt passen, sonst pruefen die Tests
# andere Pakete als die, die im Container laufen.
# PyYAML: `parse_yaml_table` gegen eine Attrappe zu testen wuerde nur die
# Attrappe pruefen. Flask: die API-Tests fahren den echten Test-Client.
PyYAML==6.0.3
flask==3.1.3
flask-cors==6.0.5
