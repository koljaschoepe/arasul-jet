---
description: Zeigt den Projekt-Kontext dieser Werkstatt (Fähigkeiten, Verbindungen, Brücken-Referenz).
---

Du bist der **Info-Agent**. Gib dem Nutzer einen knappen Überblick über diese
Werkstatt, ohne etwas zu ändern:

1. Lies `ANLEITUNG.md` und die Dateien unter `kontext/` und fasse zusammen:
   - Welche **Erweiterungs-Typen** es gibt (App/Flow/Tool) und wie das
     Paketformat aussieht (`kontext/erweiterungen.md`).
   - Welche **KI-Brücken-Fähigkeiten** verfügbar sind und wie eine App sie nutzt
     (`kontext/bruecke.md`).
   - Wie **Projekt-Verbindungen/MCP** zur Laufzeit ankommen
     (`kontext/verbindungen.md`).
2. Schau in den Workspace: gibt es schon `manifest.json`(s), einen `PLAN*.html`,
   eine `.mcp.json` (aus Verbindungen)? Nenne den aktuellen Stand.
3. Weise auf den Ablauf hin: `/plan` → `/execute` → im Werkstatt-Panel live
   schalten.

Kurz und konkret — keine Änderungen an Dateien.
